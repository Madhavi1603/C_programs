// calculate and print area and perimeter of a square

#include<stdio.h>
int main()
{
int a,area, permiter;
printf("enter a numbers");
scanf("%d",&a);
area =a*a;
permiter=4*a;
printf("area of rectangle:%d",area);
printf("permiter of rectangle:%d",permiter);
return 0;
}
